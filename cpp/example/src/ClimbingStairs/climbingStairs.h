#include <iostream>
#include <vector>
#include <math.h>

using namespace std;

class climbingStairs{
    public:
        static int naive(int n);
        static int dp(int n);
        static int formula(int n);
};
